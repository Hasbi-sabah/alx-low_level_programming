#include <stdio.h>

/**
  *main - Entry point
  *Description: idk
  *
  *Return: Always 0 (success)
  */

int main(void)
{
	char str[] = "\"Programming is like building a multilingual puzzle";

	puts(str);
	return (0);
}
